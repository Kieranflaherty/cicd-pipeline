pipeline {
	agent any
	stages {
		stage('checkout'){
			steps {
				git 'https://mgiblin@bitbucket.org/cbd23/couponservice.git'
			}
		}
		stage('Build'){
			steps {
				bat "mvn clean compile"
			}
		}
		stage('test'){
			steps {
			 	bat "mvn test"
			 	junit '**/target/surefire-reports/TEST-*.xml'
			 }
		}
		stage('Package') {
			steps {
				bat "mvn package"
			}
		}		
	}
}			